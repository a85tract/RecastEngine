"""Oracles shipped in-tree.

One so far: ``f2py-golden``, the compiled-truth oracle the ``translate``
recipe gates on. The other two the recipes name -- ``dump-replay`` for
ports, ``pinned-run`` for refactors -- arrive with their workloads.
"""
